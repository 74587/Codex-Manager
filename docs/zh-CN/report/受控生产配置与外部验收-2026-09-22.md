# 受控生产配置与外部验收记录（2026-09-22）

## 已准备

- [受控生产配置模板](../../../config/production/codexmanager.env.example)：只含地址、运行时上限和占位符，不含数据库密码、RPC token 或 provider 凭据。
- [配置说明](../../../config/production/README.md)：说明 secret manager、回环绑定、MySQL 构建和验收边界。
- [脱敏配置门控](../../../scripts/production-acceptance.ps1)：现在要求 `CODEXMANAGER_RPC_TOKEN_FILE`，会拒绝 `REPLACE_ME` / `CHANGE_ME` 等占位符；输出不包含配置值。

使用模板直接运行门控得到：`HANDOFF`，`required_variables_present=true`，`placeholder_variables=[CODEXMANAGER_DATABASE_URL]`，退出码 `2`。这证明模板不会被误当成已配置的生产环境。

## 可运行安装包

已从 GitHub 稳定发布 `v0.6.0` 下载并核对以下 Windows 产物，保存在 `artifacts/controlled-production-acceptance-20260922/`：

| 产物 | ProductVersion | SHA-256 | 签名 |
| --- | --- | --- | --- |
| `CodexManager_0.6.0_x64-setup.exe` | `0.6.0` | `8600AABB6B7E0EB3ED14F53B0952E55493A93409BBAA049B4E9C24078ACAF359` | `NotSigned` |
| `CodexManager-portable.exe` | `0.6.0` | `902B718DDD781D5FF16EBE57EDF47838EFC7B4AF3DA00F5A3B4B5253EBF5E140` | `NotSigned` |

来源提交为 `964e11a93fdb29ce4dcb2f881f2ca99b4c82938f`，对应 `release-all` 成功运行 `33953371721`，不是当前包含未提交迁移改动的工作树。该包可以作为现有稳定版本的运行基线，不能作为当前工作树的新发布或生产 GUI 验收证据。

随后在当前工作树执行 `cargo tauri build --bundles nsis` 成功（exit 0），并将产物复制为 `CodexManager-current-worktree_0.6.0_x64-setup.exe` 与 `CodexManager-current-worktree-portable.exe`。当前构建 SHA-256 分别为 `7597D5E184492FB53042219963C85EF65B7665D06012F591631BE79258801156` 和 `AD0ED700995F3A0080BA50581B44A54DAC6FFD125968B0E5141A5E9B540EAEA3`。NSIS 安装器在隔离临时目录以 `/S /NCRC /D=...` 安装，退出码为 `0`；安装后的程序启动保持运行 8 秒后再结束测试进程。两份当前工作树产物均为 `Authenticode: NotSigned`。

## 当前阻塞与 HANDOFF

- `cargo tauri info` 仍报告未检测到完整的 Visual Studio/MSVC + Windows SDK 供应链，但本次实际 NSIS 构建已成功；正式签名发布前仍需在受控构建机确认工具链来源和签名流程。
- 当前进程未提供生产数据库、provider、认证身份或完整监听端口；没有执行真实数据库读写、OAuth、provider tools/tool_calls、反向代理、WebSocket 或停服验收。
- 已发布与当前工作树的 Windows 安装器均未签名，不能据此宣称 Authenticode 或生产 GUI 通过；本次启动检查只是隔离本地 smoke test。

拿到受控配置文件路径、目标地址/端口和批准的测试窗口后，应在目标环境运行门控脚本，再按真实数据库、provider、登录/session、反向代理和关闭路径逐项记录 HTTP/浏览器证据；凭据不要粘贴到聊天或报告中。
